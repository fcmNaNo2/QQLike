# NapCat-Docker 持久化目录权限（建议与宿主机用户一致）
# macOS/Linux 可用 `id -u` / `id -g` 获取
NAPCAT_UID=1000
NAPCAT_GID=1000

# NapCatQQ API 配置
API_URL=http://napcat-account1:3000
ACCESS_TOKEN=

# 点赞目标配置
TARGET_FRIENDS=987654321
LIKE_TIMES=10

# 定时任务配置
SCHEDULE_TIME=09:00
DELAY=2

# 管理页面（可选）
ADMIN_ENABLE=true
ADMIN_PORT=8080
STATE_FILE=/app/data/state.json
